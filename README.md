# Level 4 — The API Is Broken

The `/ping` API route is failing its test.

Maybe a typo.  
Maybe something worse.  
Maybe the developer was sleepy.

## Your Task
Fix the buggy API route inside `api/server.js` so that:
- `GET /ping` returns `"pong"`
- The test suite passes

When CI turns **🟢 GREEN**, you escape Level 4.

## Hints
- Start by running the server locally
- The error is probably staring at you
